server:
  port: 8079

spring:
  application:
    name: spring-cloud-client1

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8081/eureka
    fetch-registry: true
    register-with-eureka: true
